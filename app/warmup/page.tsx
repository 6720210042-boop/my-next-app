export default function WarmupPage() {
  return (
    <div className="fade-in-up">
      <h1 className="section-title">Warmup</h1>
      <p style={{ color: 'var(--text-muted)' }}>Warmup page.</p>
    </div>
  );
}
